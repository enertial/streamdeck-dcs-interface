coverage:
  status:
    default_rules:
      if_ci_failed: success
      flag_coverage_not_uploaded_behavior: exclude
    project:
      default:
        flags:
          - Cpp
          - ReactJS
codecov:
  notify:
    after_n_builds: 2
comment:
  after_n_builds: 2

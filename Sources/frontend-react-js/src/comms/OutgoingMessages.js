/*
export function storeLastSearchQuery(searchQuery) {
  sendToPropertyInspector("storeLastSearchQuery", { searchQuery: searchQuery });
}

export function storeLastSelectedModule(module) {
  sendToPropertyInspector("storeLastSelectedModule", {
    selectedModule: module,
  });
}

export function storeConnectionSettings(
  ip_address,
  listener_port,
  send_port,
  multicast_address
) {
  sendToPropertyInspector("storeConnectionSettings", {
    ip_address: ip_address,
    listener_port: listener_port,
    send_port: send_port,
    multicast_address: multicast_address,
  });
}

export function requestModuleList(path) {
  sendToPropertyInspector("requestModuleList", { path: path });
}

export function requestControlReferenceJson(filename) {
  sendToPropertyInspector("requestControlReferenceJson", {
    filename: filename,
  });
}

export function storeButtonSettings(send_address, press_value, release_value) {
  sendToPropertyInspector("storeButtonSettings", {
    send_address: send_address,
    press_value: press_value,
    release_value: release_value,
  });
}
*/
